{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd backend && nodemon --exec './node_modules/.bin/ts-node' src/server.ts",
    "build": "cd frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "dependencies": {
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1"
  },
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1"
  }
}
